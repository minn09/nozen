# Project Conventions — nozen

> Auto-generated by sdd-init on 2026-06-12

## Code Style

- **Language**: TypeScript (strict mode enabled)
- **Formatting**: Biome — tab indentation, double quotes
- **Linting**: Biome (primary) + ESLint eslint-config-next (secondary)
- **Pre-commit**: Husky runs `bun run check` via lint-staged
- **ID Generation**: Always `crypto.randomUUID()` via `@/lib/utils/id.generateId()`
- **CSS**: Tailwind CSS 4 with `cn()` utility (tailwind-merge + clsx)

## Component Patterns

- Functional components with React 19 patterns
- shadcn/ui components in `components/ui/` (New York style)
- Radix UI primitives for accessibility
- Framer Motion for animations
- Lucide React for icons

## State Management

- Zustand 5 stores in `store/`
- Each domain has its own store file (diary, notes, tasks, UI)

## Form Handling

- React Hook Form for form state
- Zod 4 schemas for validation

## Architecture Principles

See `FEATURES.md` for full roadmap.

1. Single codebase for web and desktop
2. Offline-first with future sync layer
3. Aggregative — each phase builds on previous without breaking
4. Backwards compatible data migration

## Testing

**No test framework currently installed.** When adding tests:
- Unit/integration: vitest recommended
- Component: React Testing Library via vitest
- E2E: Playwright recommended
- TDD: Not available until a test framework is installed
